lbg_financial_overview
create a empty repo in github
open that in codespaces
keep all the codes in files
git status
git add .
git commit -m "Add initial project files, services, templates, and static assets"
git push


